#include <stdio.h>
int main(int argc, char const *argv[])
{
    int n, suma;
    printf("Te amo romi");
    scanf("%d", &n);
    suma=((n*(n+1))/2);
    printf("la suma es %d \n", suma);
    return 0;
}
